includes:
    - phpstan-baseline.neon

parameters:
    level: 0
    paths:
        - _components
        - _data
        - _icons
        - _packages
        - _themes
        - _others
    tmpDir: build/phpstan
