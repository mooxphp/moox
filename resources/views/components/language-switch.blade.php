<div>
    @livewire('language-switch')
</div>
{{-- <form action="{{route('language.switch')}}" method="POST" class="inline-block">
    @csrf
    <select name="language" onchange="this.form.submit()" class="p-2 rounded bg-gray-100 text-gray-800">
        <option value="en" {{app()->getLocale()=== 'en' ? 'selected':''}}>English</option>
        <option value="en" {{app()->getLocale()=== 'de' ? 'selected':''}}>Deutsch</option>
        <option value="en" {{app()->getLocale()=== 'fr' ? 'selected':''}}>Französisch</option>
    </select>
</form> --}}
