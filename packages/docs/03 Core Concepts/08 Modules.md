---
title: Modules
description: Modules that extend entities with extra fields (e.g. SEO).
---

# Modules

Modules extend entities with additional fields and behavior. Examples:

- **Moox SEO** — SEO fields and meta for entities

Modules are registered in configuration and can be attached to entities; the Build command can generate modules from templates.
