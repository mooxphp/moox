---
title: Updates
description: How to update Moox packages and run the installer.
---

# Updates

Update Moox in two steps:

```bash
composer update
php artisan moox:install
```

The `moox:install` command takes care of migrations, resource activation in admin panels, and related tasks.
