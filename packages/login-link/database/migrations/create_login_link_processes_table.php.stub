<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        if (Schema::hasTable('login_link_processes')) {
            return;
        }

        Schema::create('login_link_processes', function (Blueprint $table) {
            $table->id();
            $table->string('title');
            $table->string('slug')->unique();
            $table->string('context')->default('auth')->index();
            $table->string('mail_from')->nullable();
            $table->longText('content')->nullable();
            $table->string('template_key')->index();
            $table->string('handler_key')->index();
            $table->unsignedInteger('expiry_minutes')->nullable();
            $table->boolean('invalidate_prior')->default(true);
            $table->timestamps();
            $table->softDeletes();
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('login_link_processes');
    }
};
