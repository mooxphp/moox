# Changelog

## Unreleased

- Removed draft-legacy page columns (`data`, `type`, `due_at`, `status`, `custom_properties`)
- Removed unused `NewsResource` scope and empty `relations` config
- Simplified `PageFactory` and page export for CMS-only usage
