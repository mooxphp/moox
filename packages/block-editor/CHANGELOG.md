# Changelog

All notable changes to `moox/block-editor` will be documented in this file.

## Unreleased

### Added

- `docs/DEVELOPER.md` — Maintainer handbook (architecture and extension points).
- `src/Repositories/TemplateRepository` — extracted template persistence from the API controller.
- Package `AGENTS.md` with block-editor-specific agent rules.

### Changed

- `README.md` shortened to installation, quickstart, and documentation index.
- `declare(strict_types=1)` added to all remaining `src/` PHP files.
- Documentation paths corrected from `packages/moox/block-editor` to `packages/block-editor`.
- `composer.json` extended with Moox metadata and `require-dev`.
